#계수정렬은 데이터의 차이가 1,000,000 을 넘지 않을 때 효과적으로 사용 가능

count=[0]*(max(array)+1)

for i in range(len(array)):
    count[array[i]]+=1

for i in range(len(count)):
    for j in range(count[i]):
        print(i,end=' ')
